services:
  pulse:
      image: rcourtman/pulse:5.1.31
      ports:
        - 7655:7655
      volumes:
        - type: bind
          source: ${DATA_DIR}
          target: /data
      environment:
        - PULSE_AUTH_USER=${PULSE_AUTH_USER}
        - PULSE_AUTH_PASS=${PULSE_AUTH_PASS}
      networks:
        - network
      deploy:
        mode: replicated
        replicas: 1

networks:
  network:
    driver: overlay
    attachable: true
